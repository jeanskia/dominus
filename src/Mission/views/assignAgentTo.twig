{% extends "layout.twig" %}

{%block body %}
     <div class="row">
            <div class="col-lg-9">
                <form method="post" action="{{path('mission.assign',{id:item.id})}}">
                {{ csrf_input() }}
                <div class="col-md-7">
                   {{ field('name1',item.agent,"Sélectionner un Agent",{options:agents}) }}  
                </div>                
                 <div class="col-md-7">
                <button type="submit" class="btn btn-block btn-success">Enregistrer </button>    
                </div>     
                </form> 
            </div>
                                   
         {%include '@mission/menu/menu.twig'%}
    </div>
    
{% endblock%}